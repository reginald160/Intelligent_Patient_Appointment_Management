@model List<HMSPortal.Application.ViewModels.Admin.GetAdminViewModel>
@{
    ViewData["Title"] = "All Admin";
}

<link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/jquery.dataTables.min.css">
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.all.min.js"></script>



<div class="card shadow border-0 my-4">


    <div class="card-body p-4">
        <div action="container">
            <div class="row pt-4">

                <div class="col-12 text-end mb-4">
                    <a asp-controller="Admin" asp-action="Add" class="btn btn-primary mb-2">
                        <i class="bi bi-plus-circle"></i> Create New

                    </a>
                </div>
            </div>
            <table id="tblTable" class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>
                            Name
                        </th>
                        <th>
                            Role
                        </th>
                        <th>
                            Phone
                        </th>

                        <th>
                            Date Of Birth
                        </th>

                        <th>
                            Gender
                        </th>
                        <th>

                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var obj in Model)
                    {
                        <tr>
                            <td>@obj.FirstName @obj.FirstName</td>
                            <td>@obj.Role</td>
                            <td>@obj.Phone</td>
                            <td>@obj.DateOfBirth</td>
                            <td>@obj.Gender</td>

                            <td class="w-75 btn-group" role="group">
                                <a asp-controller="Admin" asp-action="Detail" asp-route-Id="@obj.Id" class="btn btn-secondary">
                                    View
                                </a>
                                <br />

                                <button type="button" class="btn btn-danger mx-2" onclick="confirmDelete('@obj.Id')">
                                    Delete
                                </button>
                                @if(obj.IsLocked)
                                {
                                    <button type="button" class="btn btn-primary mx-2" onclick="confirmUnLock('@obj.Id')">
                                      Unclock 
                                    </button>
                                }
                                else
                                {
                                    <button type="button" class="btn btn-secondary mx-2" onclick="confirmLock('@obj.Id')">
                                        Lock
                                    </button>
                                }
                            </td>
                        </tr>
                    }

                </tbody>

            </table>

        </div>
    </div>

</div>

@section Scripts {

    <script src="~/hmsproj-js/admin.js"></script>

}


